#include<stdio.h>
int main()
{
	int n,r,s=0,t;
	printf("enter a num : ");
	scanf("%d",&n);
	t=n;
	while(n>0)
	{
		r=n%10;
		s=s+(r*r*r);
		n=n/10;
	}
	if(t==s)
	printf("%d is an armstrong",t);
	else
	printf("%d is an not armstrong",t);
}
